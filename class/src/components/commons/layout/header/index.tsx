import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 150px;
  background-color: orange;
  font-family: "myFont";
`;
export default function LayoutHeader() {
  return <Wrapper>header space desu.</Wrapper>;
}
